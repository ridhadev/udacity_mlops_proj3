# This workflow will install Python dependencies, run tests and lint with a single version of Python
# For more information see: https://help.github.com/actions/language-and-framework-guides/using-python-with-github-actions

name: Python application

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

permissions:
  contents: read

jobs:
  build:

    runs-on: ubuntu-latest
    environment: DEV
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Install AWS CLI
      
      run: |
        aws --profile default configure set aws_access_key_id AKIAWCQCTJRUPM47D2RA
        aws --profile default configure set aws_secret_access_key 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
        aws --profile default configure set output json
        aws --profile default configure set region us-east-1
        aws s3 ls
 
